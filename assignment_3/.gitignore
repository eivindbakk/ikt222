# --- Python / build ---__pycache__/*.py[cod]*.pyo*.pyd*.so*.egg-info/.eggs/build/dist/pip-wheel-metadata/*.log# --- Virtual envs ---.venv/venv/env/ENV/# --- Flask / SQLite data ---instance/*.sqlite*.sqlite3*.dbauth.db# --- Environment files ---.env.env.*!.env.example# --- Tests / tooling caches ---.pytest_cache/.tox/.mypy_cache/.coverage.coverage.*htmlcov/.cache/# --- IDE / OS ---.vscode/.idea/*.code-workspace.DS_StoreThumbs.db